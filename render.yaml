services:
  - type: web
    name: area-cliente
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: CLIENT_EMAIL
        sync: false
      - key: CLIENT_PASSWORD_HASH
        sync: false
